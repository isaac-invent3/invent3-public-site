import type { Metadata } from 'next';
import FeedbackOverview from '~/lib/components/Feedback';

export const metadata: Metadata = {
  title: 'Feedback Overview',
};

export default FeedbackOverview;
