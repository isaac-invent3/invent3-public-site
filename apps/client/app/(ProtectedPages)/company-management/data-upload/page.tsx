import { Metadata } from 'next';
import DataUpload from '~/lib/components/CompanyManagement/DataUpload';

export const metadata: Metadata = {
  title: 'Company Management - Data upload',
};

export default DataUpload;
