import type { Metadata } from 'next';
import ReportAnalytics from '~/lib/components/ReportAnalytics';

export const metadata: Metadata = {
  title: 'Report and Analytics',
};

export default ReportAnalytics;
