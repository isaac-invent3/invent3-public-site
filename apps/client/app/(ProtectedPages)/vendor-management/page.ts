import type { Metadata } from 'next';
import VendorManagement from '~/lib/components/VendorManagement';

export const metadata: Metadata = {
  title: 'Vendor Management',
};

export default VendorManagement;
