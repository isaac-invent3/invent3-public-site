import type { Metadata } from 'next';
import TemplateManagement from '~/lib/components/TemplateManagement';

export const metadata: Metadata = {
  title: 'Template Management',
};

export default TemplateManagement;
