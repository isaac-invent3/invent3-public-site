import type { Metadata } from 'next';
import UserManagement from '~/lib/components/UserManagement';

export const metadata: Metadata = {
  title: 'User Management',
};

export default UserManagement;
