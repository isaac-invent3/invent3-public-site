import type { Metadata } from 'next';
import TaskManagement from '~/lib/components/TaskManagement';

export const metadata: Metadata = {
  title: 'Task Management',
};

export default TaskManagement;
