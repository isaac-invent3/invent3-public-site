import type { Metadata } from 'next';
import TicketBalancing from '~/lib/components/TicketManagement/TicketBalancing';

export const metadata: Metadata = {
  title: 'Workload Balancing',
};

export default TicketBalancing;
