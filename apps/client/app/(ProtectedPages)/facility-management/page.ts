import type { Metadata } from 'next';
import FacilityManagement from '~/lib/components/FacilityManagement';

export const metadata: Metadata = {
  title: 'Facility Management',
};

export default FacilityManagement;
