import ApprovalNode from './ApprovalNode';
import StackJoinerNode from './StackJoinerNode';

const nodeTypes = {
  approvalNode: ApprovalNode,
  stackJoiner: StackJoinerNode,
} as any;

export default nodeTypes;
