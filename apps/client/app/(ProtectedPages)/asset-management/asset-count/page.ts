import type { Metadata } from 'next';
import AssetCount from '~/lib/components/AssetManagement/AssetCount';

export const metadata: Metadata = {
  title: 'Asset Count',
};

export default AssetCount;
