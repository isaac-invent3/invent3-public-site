import type { Metadata } from 'next';
import TicketsDashboard from '~/lib/components/Dashboard/TicketsDashboard';

export const metadata: Metadata = {
  title: 'Tickets Dashboard',
};

export default TicketsDashboard;
