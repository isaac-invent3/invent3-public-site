import type { Metadata } from 'next';
import Transportation from '@/lib/components//Sectors/Transportation';

export const metadata: Metadata = {
  title: 'Sector - Transportation',
};

export default Transportation;
