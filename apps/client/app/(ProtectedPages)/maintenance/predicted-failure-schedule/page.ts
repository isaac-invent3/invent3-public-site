import type { Metadata } from 'next';
import PredictedFailureSchedule from '~/lib/components/Maintenance/PredictedFailureSchedule';

export const metadata: Metadata = {
  title: 'Predicted Failures Schedule',
};

export default PredictedFailureSchedule;
