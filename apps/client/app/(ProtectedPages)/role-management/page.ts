import type { Metadata } from 'next';
import RoleManagement from '~/lib/components/RoleManagement';

export const metadata: Metadata = {
  title: 'Role Management',
};

export default RoleManagement;
