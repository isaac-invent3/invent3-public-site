import type { Metadata } from 'next';
import ApprovalWorkFlow from '~/lib/components/ApprovalWorkflow';

export const metadata: Metadata = {
  title: 'Approval Request',
};

export default ApprovalWorkFlow;
