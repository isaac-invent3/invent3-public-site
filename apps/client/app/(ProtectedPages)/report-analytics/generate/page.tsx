import type { Metadata } from 'next';
import GenerateReport from '~/lib/components/ReportAnalytics/GenerateReport';

export const metadata: Metadata = {
  title: 'Report and Analytics - Generate Report',
};

export default GenerateReport;
