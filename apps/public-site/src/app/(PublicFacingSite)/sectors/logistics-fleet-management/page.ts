import type { Metadata } from 'next';
import FleetLogistics from '@/lib/components//Sectors/FleetLogistics';

export const metadata: Metadata = {
  title: 'Sector - Fleet & Logistics',
};

export default FleetLogistics;
