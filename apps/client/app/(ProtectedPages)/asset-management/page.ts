import type { Metadata } from 'next';
import AssetManagement from '~/lib/components/AssetManagement';

export const metadata: Metadata = {
  title: 'Asset Management',
};

export default AssetManagement;
