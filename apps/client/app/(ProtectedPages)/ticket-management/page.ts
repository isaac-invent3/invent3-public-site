import type { Metadata } from 'next';
import TicketManagement from '~/lib/components/TicketManagement';

export const metadata: Metadata = {
  title: 'Ticket Management',
};

export default TicketManagement;
